# Download raw directory data from NCDPI

Downloads the North Carolina school directory Excel file(s).

## Usage

``` r
get_raw_directory(directory_type = "all")
```

## Arguments

- directory_type:

  Type of directory ("private_schools", "all")

## Value

List with raw data frames for each directory type requested
