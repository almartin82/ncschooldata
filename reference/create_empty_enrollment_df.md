# Create empty enrollment data frame with expected structure

Used as fallback when downloads fail.

## Usage

``` r
create_empty_enrollment_df(level)
```

## Arguments

- level:

  "lea" or "school"

## Value

Empty data frame with expected columns
